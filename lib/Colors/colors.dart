import 'package:flutter/material.dart';

const primaryColor = Colors.pink;
const accentColor = Colors.amber;

var colorScheme = ColorScheme.fromSwatch(primarySwatch: primaryColor).
copyWith(secondary: accentColor);

const lightYellow = Color.fromRGBO(255, 254, 229, 1);

